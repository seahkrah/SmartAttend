# Frontend configuration (apps/frontend). Copy to apps/frontend/.env.
# The backend's settings are documented in apps/backend/.env.example.
#
# This file used to list MFA, superadmin-MFA and rate-limit settings that no
# code read; they are gone so that nobody believes they are protected by them.

# Where the browser reaches the API.
VITE_API_BASE_URL=http://localhost:5000/api
