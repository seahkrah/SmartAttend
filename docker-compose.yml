version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: jjelotech-db
    environment:
      POSTGRES_USER: jjelotech_user
      POSTGRES_PASSWORD: jjelotech_password
      POSTGRES_DB: jjelotech_db
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U jjelotech_user"]
      interval: 10s
      timeout: 5s
      retries: 5

  backend:
    build:
      context: ./apps/backend
      dockerfile: Dockerfile
    container_name: jjelotech-backend
    environment:
      NODE_ENV: development
      PORT: 5000
      DATABASE_URL: postgresql://jjelotech_user:jjelotech_password@postgres:5432/jjelotech_db
    ports:
      - "5000:5000"
    depends_on:
      postgres:
        condition: service_healthy
    command: node dist/server.js

  frontend:
    build:
      context: ./apps/frontend
      dockerfile: Dockerfile
    container_name: jjelotech-frontend
    environment:
      VITE_API_URL: http://localhost:5000/api
    ports:
      - "3000:3000"
    depends_on:
      - backend

volumes:
  postgres_data:
